<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sky CU"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Width="380" Height="180"
        WindowStartupLocation="Manual">
  <Border CornerRadius="16" Background="#FFFFFF" Padding="20,16">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- Header -->
      <TextBlock Grid.Row="0" Text="FastCUA is using your computer"
                 Foreground="#1D1D1F" FontSize="15" FontWeight="SemiBold"
                 TextWrapping="Wrap" Margin="0,0,0,8"/>

      <!-- Current action -->
      <TextBlock Grid.Row="1" x:Name="Text0" Text="Idle"
                 Foreground="#6E6E73" FontSize="13"
                 TextWrapping="Wrap" Margin="0,0,0,6"/>

      <!-- Last action -->
      <TextBlock Grid.Row="2" x:Name="Text1" Text=""
                 Foreground="#8E8E93" FontSize="11"
                 TextWrapping="Wrap" Margin="0,0,0,8"/>

      <!-- Bottom: stop + input -->
      <Grid Grid.Row="3" Margin="0,8,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="InputBox"
                 Background="#F5F5F7" Foreground="#1D1D1F"
                 BorderBrush="#E5E5EA" BorderThickness="1"
                 Padding="8,6" FontSize="12" Height="28"
                 VerticalContentAlignment="Center"/>
        <Button x:Name="StopBtn" Grid.Column="1"
                Content="Stop" FontSize="12" Width="48" Height="28"
                Background="#E5E5EA" Foreground="#FF3B30"
                BorderBrush="Transparent" BorderThickness="0" Cursor="Hand" Margin="8,0,0,0"/>
      </Grid>
    </Grid>
  </Border>
</Window>
